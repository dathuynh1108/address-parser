# Repo structure
    The repo is structured as follows:
    
    - `fuzz/`: Contains the address parser implementation and related utilities.
    - `ner/`: Contains scripts and modules for building and preparing datasets for Named Entity Recognition (NER) tasks.
    - `tests/`: Contains unit tests for the various components of the project.

